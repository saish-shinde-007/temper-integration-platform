{
  "name": "integration-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "pnpm -r build",
    "demo": "docker compose up -d temporal mock-system-a mock-system-b mock-sftp egress-proxy && pnpm -r --parallel dev",
    "stop": "docker compose down && pkill -f 'pnpm.*dev' || true",
    "deploy": "bash scripts/deploy-gcp.sh",
    "test": "pnpm -r test",
    "setup": "pnpm install && pnpm -r build && docker pull node:20-slim"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "tsx": "^4.19.0",
    "@types/node": "^22.5.0"
  }
}
